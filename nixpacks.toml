[phases.setup]
nixPkgs = ['python310', 'pip']

[phases.install]
cmds = ['pip install -r requirements.txt']

[phases.build]
cmds = ['echo "Build fase completa"']

[start]
cmd = 'gunicorn web_app.app:app --bind 0.0.0.0:$PORT'